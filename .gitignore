node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local dev sync store & its rolling backups/permanent vault (runtime-generated user data —
# never something to commit)
sync_store*.json
